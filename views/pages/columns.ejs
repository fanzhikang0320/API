<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <%- include('../templates/public-style.ejs') %>
</head>
<body>
    <div class="layui-layout layui-layout-admin page-main">
        <%- include('../templates/header.ejs', { userInfo }) %>
        
        <div class="layui-side layui-bg-black">
          <div class="layui-side-scroll">
            <!-- 左侧导航区域（可配合layui已有的垂直导航） -->
            <%- include('../templates/navigation.template.ejs') %>
          </div>
        </div>
        
        <div class="layui-body">
            <div class="layui-row main-container">
                <div class="layui-row">
                  <button class="layui-btn add-columns-btn"><i class="layui-icon layui-icon-addition"></i>添加栏目</button>
                </div>
                  <table class="layui-table" id="columns-table" lay-filter="columns"></table>
              </div>
        </div>
        
        <div class="layui-footer">
          <!-- 底部固定区域 -->
          底部固定区域
        </div> 
      </div>
      <script type="text/html" id="handle-bar">
        <a class="layui-btn layui-btn-sm" lay-event="edit">编辑</a>
        <a class="layui-btn layui-btn-danger layui-btn-sm" lay-event="del">删除</a>
      </script>
      <%- include('../templates/public-js.ejs') %>
      <script src="/js/columns.js"></script>
</body>

</html>